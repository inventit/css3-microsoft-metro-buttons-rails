RSpec.describe Css3::Microsoft::Metro::Buttons::Rails do
  it "has a version number" do
    expect(Css3::Microsoft::Metro::Buttons::Rails::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
